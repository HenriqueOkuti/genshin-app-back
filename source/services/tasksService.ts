async function handleFetchUserTasks(userId: number) {
  //search and return
  const something = 'null';

  return something;
}

const tasksService = {
  handleFetchUserTasks,
};

export { tasksService };
